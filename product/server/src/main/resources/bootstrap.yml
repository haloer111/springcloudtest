spring:
  application:
    name: product
  redis:
    host: 192.168.0.107
    port: 6379
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

